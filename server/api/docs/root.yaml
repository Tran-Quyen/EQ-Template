schemes:
  - http
  - https
securityDefinitions:
  Bearer:
    type: apiKey
    name: Authorization
    in: header
    description: 'Put `Bearer ` before your access_token string. Ex: <br /> Bearer access_token'